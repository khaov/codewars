// Solution:

var templateStrings = function(noun, adjective) {
  return `${noun} are ${adjective}`;
}

// Sample Tests:

templateStrings('Animals', 'Good'); // 'Animals are Good'
templateStrings('You', 'special');  // 'You are special'
templateStrings('lives', 'frozen'); // 'lives are frozen'
