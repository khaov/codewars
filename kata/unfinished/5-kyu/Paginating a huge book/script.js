// Solution:

function pageDigits(pages) {
  let length = pages.toString().length;

  if (length === 1) {
    return pages;
  } else {
    let digitsArray = [9];
  
    for (let number = 1; number <= length; number++) {

    }
  }
}

// Sample Tests:

pageDigits(4n) // 4n
pageDigits(12n) // 15n
pageDigits(100n) // 192n
