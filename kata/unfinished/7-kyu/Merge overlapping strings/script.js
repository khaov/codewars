// Solution:

function mergeStrings(first, second) {
  //const longestString = first.length >= second.length ? first.length : second.length;

  // for (let j = 0; j < second.length; j++) {

  // }



  first.split('')
  second.split('')



  first.includes('cde')

  let overlap = [];

  let firstIndex = 0;
  let secondIndex = 0;


  for (let i = 0; i < first.length; i++) {

  }
  const firstString = first.split('').reverse().join('')

  let overlapLength = 0;


  return overlap
}

// Sample Tests:

mergeStrings('abcde', 'cdefgh') // 'abcdefgh'
mergeStrings('abaab', 'aabab') // 'abaabab'
mergeStrings('abc', 'def') // 'abcdef'
mergeStrings('abc', 'abc') // 'abc'
